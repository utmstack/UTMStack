# SonicWall Firewall — version 4.1.0
#
# Formats supported
#   - SonicOS syslog KV:  <pri>  id=firewall sn=... time="..." fw=... msg="..." src=IP:PORT:IF ...
#   - SonicOS syslog CEF: <pri> CEF:0|SonicWall|SonicOS|...|EventID|EventName|Sev|extension
#     where `extension` carries the same k=v pairs as the KV variant.
#
# References
# - https://www.sonicwall.com/techdocs/pdf/sonicos-6-5-4-log-events-reference-guide.pdf
# - https://www.sonicwall.com/techdocs/pdf/SonicOS-X_7.0.1_LogEvents_ReferenceGuide.pdf

pipeline:
  - dataTypes:
      - firewall-sonicwall
    steps:
      # -------------------------------------------------------------------
      # Syslog header (<pri>)
      # -------------------------------------------------------------------
      - grok:
          source: raw
          patterns:
            - fieldName: log.syslogPri
              pattern: '\<{{.integer}}\>'

      - trim:
          function: prefix
          substring: "<"
          fields:
            - log.syslogPri
      - trim:
          function: suffix
          substring: ">"
          fields:
            - log.syslogPri

      # -------------------------------------------------------------------
      # CEF header (only when the log line contains "CEF:")
      # The grok step matches each pattern alone and rejects an empty match,
      # so header fields use a non-empty pattern instead of a lazy one.
      # -------------------------------------------------------------------
      - grok:
          source: raw
          patterns:
            - fieldName: ""
              pattern: '{{.data}}CEF:'
            - fieldName: log.cefVersion
              pattern: '{{.integer}}'
            - fieldName: ""
              pattern: '\|'
            - fieldName: log.deviceVendor
              pattern: '(?:[^|\\]|\\.)+'
            - fieldName: ""
              pattern: '\|'
            - fieldName: log.deviceProduct
              pattern: '(?:[^|\\]|\\.)+'
            - fieldName: ""
              pattern: '\|'
            - fieldName: log.deviceVersion
              pattern: '(?:[^|\\]|\\.)+'
            - fieldName: ""
              pattern: '\|'
            - fieldName: log.eventCode
              pattern: '(?:[^|\\]|\\.)+'
            - fieldName: ""
              pattern: '\|'
            - fieldName: log.eventName
              pattern: '(?:[^|\\]|\\.)+'
            - fieldName: ""
              pattern: '\|'
            - fieldName: log.cefSeverity
              pattern: '(?:[^|\\]|\\.)+'
            - fieldName: ""
              pattern: '\|'
          where: contains("raw", "CEF:")

      # -------------------------------------------------------------------
      # Key/Value pass over raw
      # -------------------------------------------------------------------
      - kv:
          source: raw
          fieldSplit: " "
          valueSplit: "="

      # -------------------------------------------------------------------
      # Rescue quoted / space-bearing values.
      # -------------------------------------------------------------------
      - delete:
          fields:
            - log.msg
          where: contains("raw", "msg=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(msg=)'
            - fieldName: log.message
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "msg=\"")

      - delete:
          fields:
            - log.note
          where: contains("raw", "note=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(note=)'
            - fieldName: log.note
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "note=\"")

      - delete:
          fields:
            - log.Category
          where: contains("raw", "Category=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(Category=)'
            - fieldName: log.contentCategory
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "Category=\"")

      - delete:
          fields:
            - log.rule
          where: contains("raw", "rule=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(rule=)'
            - fieldName: log.rule
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "rule=\"")

      - delete:
          fields:
            - log.sess
          where: contains("raw", "sess=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(sess=)'
            - fieldName: log.sessionType
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "sess=\"")

      - delete:
          fields:
            - log.vpnpolicy
          where: contains("raw", "vpnpolicy=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(vpnpolicy=)'
            - fieldName: log.vpnPolicy
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "vpnpolicy=\"")

      - delete:
          fields:
            - log.usr
          where: contains("raw", "usr=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(usr=)'
            - fieldName: log.userRaw
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "usr=\"")

      - delete:
          fields:
            - log.uuid
          where: contains("raw", "uuid=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(uuid=)'
            - fieldName: log.eventUuid
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "uuid=\"")

      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(fw_action=)'
            - fieldName: log.actionRaw
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "fw_action=\"")

      - delete:
          fields:
            - log.appName
          where: contains("raw", "appName='")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(appName=)'
            - fieldName: log.appName
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "appName='")

      - delete:
          fields:
            - log.time
          where: contains("raw", "time=\"")
      - grok:
          source: raw
          patterns:
            - fieldName: log.grokTrash
              pattern: '{{.data}}(time=)'
            - fieldName: log.deviceTimeRaw
              pattern: '{{.data}}({{.word}}=)'
            - fieldName: log.grokTrash
              pattern: '{{.greedy}}'
          where: contains("raw", "time=\"")

      # -------------------------------------------------------------------
      # Strip the trailing `word=` that the rescue groks include in
      # the captured value.
      # -------------------------------------------------------------------
      - grok:
          source: log.message
          patterns:
            - fieldName: log.message
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.message")

      - grok:
          source: log.note
          patterns:
            - fieldName: log.note
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.note")

      - grok:
          source: log.contentCategory
          patterns:
            - fieldName: log.contentCategory
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.contentCategory")

      - grok:
          source: log.rule
          patterns:
            - fieldName: log.rule
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.rule")

      - grok:
          source: log.sessionType
          patterns:
            - fieldName: log.sessionType
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.sessionType")

      - grok:
          source: log.vpnPolicy
          patterns:
            - fieldName: log.vpnPolicy
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.vpnPolicy")

      - grok:
          source: log.userRaw
          patterns:
            - fieldName: log.userRaw
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.userRaw")

      - grok:
          source: log.eventUuid
          patterns:
            - fieldName: log.eventUuid
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.eventUuid")

      - grok:
          source: log.actionRaw
          patterns:
            - fieldName: log.actionRaw
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.actionRaw")

      - grok:
          source: log.appName
          patterns:
            - fieldName: log.appName
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.appName")

      - grok:
          source: log.deviceTimeRaw
          patterns:
            - fieldName: log.deviceTimeRaw
              pattern: '{{.greedy}}{{.space}}'
            - fieldName: log.grokTrash
              pattern: '{{.word}}(=)'
          where: exists("log.deviceTimeRaw")

      # -------------------------------------------------------------------
      # Split src / dst  =>  ip[:port[:iface]], ip:port or ip::iface.
      # Each shape has its own step: a step writes only when all its patterns match.
      # -------------------------------------------------------------------
      # ip:port:interface
      - grok:
          source: log.src
          patterns:
            - fieldName: origin.ip
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: origin.port
              pattern: '{{.integer}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: log.sourceInterface
              pattern: '{{.word}}'
          where: regexMatch("log.src", "^[0-9.]+:[0-9]+:.")
      # ip:port, used by VPN negotiation events
      - grok:
          source: log.src
          patterns:
            - fieldName: origin.ip
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: origin.port
              pattern: '{{.integer}}'
          where: regexMatch("log.src", "^[0-9.]+:[0-9]+$")
      # ip::interface, used by user login events
      - grok:
          source: log.src
          patterns:
            - fieldName: origin.ip
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: '::'
            - fieldName: log.sourceInterface
              pattern: '{{.word}}'
          where: regexMatch("log.src", "^[0-9.]+::.")

      - delete:
          fields:
            - log.src
          where: exists("origin.ip")

      # A bare address; any other value stays under its vendor key.
      - rename:
          from:
            - log.src
          to: origin.ip
          where: inCIDR("log.src", "0.0.0.0/0")

      # ip:port:interface
      - grok:
          source: log.dst
          patterns:
            - fieldName: target.ip
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: target.port
              pattern: '{{.integer}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: log.targetInterface
              pattern: '{{.word}}'
          where: regexMatch("log.dst", "^[0-9.]+:[0-9]+:.")
      # ip:port, used by VPN negotiation events
      - grok:
          source: log.dst
          patterns:
            - fieldName: target.ip
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: target.port
              pattern: '{{.integer}}'
          where: regexMatch("log.dst", "^[0-9.]+:[0-9]+$")
      # ip::interface, used by user login events
      - grok:
          source: log.dst
          patterns:
            - fieldName: target.ip
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: '::'
            - fieldName: log.targetInterface
              pattern: '{{.word}}'
          where: regexMatch("log.dst", "^[0-9.]+::.")

      - delete:
          fields:
            - log.dst
          where: exists("target.ip")

      # A bare address; any other value stays under its vendor key.
      - rename:
          from:
            - log.dst
          to: target.ip
          where: inCIDR("log.dst", "0.0.0.0/0")

      - grok:
          source: log.natSrc
          patterns:
            - fieldName: log.natSourceIp
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: log.natSourcePort
              pattern: '{{.integer}}'
          where: contains("log.natSrc", ":")

      - delete:
          fields:
            - log.natSrc
          where: contains("log.natSrc", ":")

      - grok:
          source: log.natDst
          patterns:
            - fieldName: log.natTargetIp
              pattern: '{{.ipv4}}'
            - fieldName: ""
              pattern: ':'
            - fieldName: log.natTargetPort
              pattern: '{{.integer}}'
          where: contains("log.natDst", ":")

      - delete:
          fields:
            - log.natDst
          where: contains("log.natDst", ":")

      # -------------------------------------------------------------------
      # Split protocol => transport / service
      # -------------------------------------------------------------------
      - grok:
          source: log.proto
          patterns:
            - fieldName: protocol
              pattern: '{{.word}}'
            - fieldName: ""
              pattern: '/'
            - fieldName: log.appProto
              pattern: '{{.notSpace}}'
          where: contains("log.proto", "/")

      - delete:
          fields:
            - log.proto
          where: contains("log.proto", "/")

      - rename:
          from:
            - log.proto
          to: protocol

      # -------------------------------------------------------------------
      # Canonical + human-readable renames
      # -------------------------------------------------------------------
      - rename:
          from:
            - log.dstname
          to: target.host
      - rename:
          from:
            - log.srcMac
          to: origin.mac
      - rename:
          from:
            - log.dstMac
          to: target.mac
      - rename:
          from:
            - log.smac
          to: origin.mac
      - rename:
          from:
            - log.dmac
          to: target.mac
      - rename:
          from:
            - log.sent
          to: origin.bytesSent
      - rename:
          from:
            - log.rcvd
          to: origin.bytesReceived
      - rename:
          from:
            - log.spkt
          to: origin.packagesSent
      - rename:
          from:
            - log.rpkt
          to: origin.packagesReceived
      - rename:
          from:
            - log.id
          to: log.sourceId
      - rename:
          from:
            - log.sn
          to: log.deviceSerial
      - rename:
          from:
            - log.fw
          to: log.deviceIp
      - rename:
          from:
            - log.pri
          to: log.priority
      - rename:
          from:
            - log.c
          to: log.messageClass
      - rename:
          from:
            - log.gcat
          to: log.groupCategoryId
      - rename:
          from:
            - log.m
          to: log.eventCode
      - rename:
          from:
            - log.n
          to: log.eventSerial
      - rename:
          from:
            - log.app
          to: log.appId
      - rename:
          from:
            - log.srcZone
          to: log.sourceZone
      - rename:
          from:
            - log.dstZone
          to: log.targetZone
      - rename:
          from:
            - log.code
          to: log.contentCode
      - rename:
          from:
            - log.arg
          to: log.webPath
      - rename:
          from:
            - log.dpi
          to: log.dpiEnabled
      - rename:
          from:
            - log.ipscat
          to: log.ipsCategory
      - rename:
          from:
            - log.ipspri
          to: log.ipsPriority
      - rename:
          from:
            - log.spycat
          to: log.spywareCategory
      - rename:
          from:
            - log.spypri
          to: log.spywarePriority
      - rename:
          from:
            - log.af_service
          to: log.appFirewallService
      - rename:
          from:
            - log.af_type
          to: log.appFirewallType
      - rename:
          from:
            - log.result
          to: log.threatResult
      - rename:
          from:
            - log.cs6
          to: log.threatContext

      # fw_action is often the last key, which the rescue above cannot bound.
      # Fall back to the KV value; go-sdk v1.1.35 and later keep the underscore.
      - rename:
          from:
            - log.fw_action
            - log.fwaction
          to: log.actionRaw
          where: '!exists("log.actionRaw")'

      # -------------------------------------------------------------------
      # Strip residual quotes / apostrophes from rescued values.
      # -------------------------------------------------------------------
      - trim:
          function: prefix
          substring: '"'
          fields:
            - log.message
            - log.note
            - log.contentCategory
            - log.rule
            - log.sessionType
            - log.vpnPolicy
            - log.eventUuid
            - log.deviceTimeRaw
            - log.actionRaw
            - log.userRaw
      - trim:
          function: suffix
          substring: '"'
          fields:
            - log.message
            - log.note
            - log.contentCategory
            - log.rule
            - log.sessionType
            - log.vpnPolicy
            - log.eventUuid
            - log.deviceTimeRaw
            - log.actionRaw
            - log.userRaw
      - trim:
          function: prefix
          substring: "'"
          fields:
            - log.appName
      - trim:
          function: suffix
          substring: "'"
          fields:
            - log.appName

      - rename:
          from:
            - log.actionRaw
          to: action
      - rename:
          from:
            - log.userRaw
          to: origin.user

      # -------------------------------------------------------------------
      # Type casts
      # -------------------------------------------------------------------
      - cast:
          fields:
            - origin.port
            - target.port
            - log.natSourcePort
            - log.natTargetPort
          to: int
      - cast:
          fields:
            - origin.bytesSent
            - origin.bytesReceived
          to: float
      - cast:
          fields:
            - origin.packagesSent
            - origin.packagesReceived
          to: int
      - cast:
          fields:
            - log.groupCategoryId
            - log.eventCode
            - log.priority
          to: string

      # -------------------------------------------------------------------
      # actionResult (canonical vocabulary)
      # -------------------------------------------------------------------
      - add:
          function: string
          params:
            key: actionResult
            value: success
          where: equals("action", "forward")
      - add:
          function: string
          params:
            key: actionResult
            value: denied
          where: oneOf("action", ["drop", "dropped", "deny", "denied", "block", "blocked"])
      # Events without a firewall decision, by the meaning of their message:
      # 29, 31, 236, 237, 238 and 1080 "... login allowed"; 942 "IKEv2
      # Authentication successful"; 978 "IKEv2 negotiation complete".
      - add:
          function: string
          params:
            key: actionResult
            value: success
          where: '!exists("actionResult") && oneOf("log.eventCode", ["29", "31", "236", "237", "238", "1080", "942", "978"])'
      # 537 "Connection Closed" follows an allowed connection; only one that
      # received data back completed. Without received bytes it keeps no outcome.
      - add:
          function: string
          params:
            key: actionResult
            value: success
          where: '!exists("actionResult") && equals("log.eventCode", "537") && greaterThan("origin.bytesReceived", 0)'
      # 32, 33 and 200 "... login denied due to bad credentials"; 34 "Pending
      # login timed out"; 140 "XAUTH Failed ... Authentication failure"; 243, 244,
      # 746 and 747 RADIUS or LDAP failures; 1035 "password expired"; 1117 "SSO
      # probe failed"; 402, 658, 953, 967 and 1305 failed IKE negotiations; and
      # any other "... denied due to bad credentials" message.
      - add:
          function: string
          params:
            key: actionResult
            value: failure
          where: '!exists("actionResult") && (oneOf("log.eventCode", ["32", "33", "34", "140", "200", "243", "244", "746", "747", "1035", "1117", "402", "658", "953", "967", "1305"]) || contains("log.message", "denied due to bad credentials"))'
      # 986 "User login denied - not allowed by Policy rule"; 1079 "SSLVPN
      # service is not allowed on <zone>".
      - add:
          function: string
          params:
            key: actionResult
            value: denied
          where: '!exists("actionResult") && oneOf("log.eventCode", ["986", "1079"])'

      # -------------------------------------------------------------------
      # Group category human label from log.groupCategoryId (1..17)
      # -------------------------------------------------------------------
      - add:
          function: string
          params:
            key: log.groupCategory
            value: System
          where: equals("log.groupCategoryId", "1")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Log
          where: equals("log.groupCategoryId", "2")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Security Services
          where: equals("log.groupCategoryId", "3")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Users
          where: equals("log.groupCategoryId", "4")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Firewall Settings
          where: equals("log.groupCategoryId", "5")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Network
          where: equals("log.groupCategoryId", "6")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: VPN
          where: equals("log.groupCategoryId", "7")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: High Availability
          where: equals("log.groupCategoryId", "8")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: 3G/4G, Modem, and Module
          where: equals("log.groupCategoryId", "9")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Firewall
          where: equals("log.groupCategoryId", "10")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Wireless
          where: equals("log.groupCategoryId", "11")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: VoIP
          where: equals("log.groupCategoryId", "12")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: SSL VPN
          where: equals("log.groupCategoryId", "13")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Anti-Spam
          where: equals("log.groupCategoryId", "14")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: WAN Acceleration
          where: equals("log.groupCategoryId", "15")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: SD-WAN
          where: equals("log.groupCategoryId", "16")
      - add:
          function: string
          params:
            key: log.groupCategory
            value: Multi-Tenancy
          where: equals("log.groupCategoryId", "17")

      # -------------------------------------------------------------------
      # Severity from syslog priority (SonicWall log.priority = 0..7)
      # -------------------------------------------------------------------
      - add:
          function: string
          params:
            key: severity
            value: critical
          where: oneOf("log.priority", ["0", "1", "2"])
      - add:
          function: string
          params:
            key: severity
            value: error
          where: equals("log.priority", "3")
      - add:
          function: string
          params:
            key: severity
            value: warning
          where: equals("log.priority", "4")
      - add:
          function: string
          params:
            key: severity
            value: info
          where: oneOf("log.priority", ["5", "6"])
      - add:
          function: string
          params:
            key: severity
            value: debug
          where: equals("log.priority", "7")

      # -------------------------------------------------------------------
      # Geolocation
      # -------------------------------------------------------------------
      - dynamic:
          plugin: com.utmstack.geolocation
          params:
            source: origin.ip
            destination: origin.geolocation
          where: exists("origin.ip")

      - dynamic:
          plugin: com.utmstack.geolocation
          params:
            source: target.ip
            destination: target.geolocation
          where: exists("target.ip")

      # -------------------------------------------------------------------
      # 13. Cleanup
      # -------------------------------------------------------------------
      - delete:
          fields:
            - log.syslogPri
            - log.cefVersion
            - log.fwaction
            - log.fw_action
            - log.grokTrash
