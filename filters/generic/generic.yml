# Generic filter, version 2.0.1
# Allow processing of any one line json or syslog logs 
pipeline:
  - dataTypes:
      - generic
    steps:
      - grok:
          patterns:
            - fieldName: log.message
              pattern: '(.*)'
          source: raw
      - json:
          source: log.message
          where: 'startsWith("log.message", "{")'