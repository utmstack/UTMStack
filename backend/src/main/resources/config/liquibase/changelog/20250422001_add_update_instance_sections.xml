<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <changeSet id="20250422001" author="Manuel">
        <sql>
            <![CDATA[
                INSERT INTO public.utm_configuration_section (
                    id,
                    section,
                    description,
                    short_name,
                    section_active
                ) VALUES
                    (
                        8,
                        'Instance Update Frequency',
                        'This section allows you to configure the frequency of updates for the system instance.',
                        'INSTANCE_UPDATE_FREQUENCY',
                        true
                    );

                INSERT INTO public.utm_configuration_parameter (
                    id,
                    section_id,
                    conf_param_short,
                    conf_param_large,
                    conf_param_description,
                    conf_param_value,
                    conf_param_required,
                    conf_param_datatype,
                    modification_time,
                    modification_user,
                    conf_param_option,
                    conf_param_regexp
                ) VALUES
                    (36, 8, 'utmstack.instance.update.frequency', 'Update Frequency',
                     'Configuration parameter for selecting the frequency of system instance updates. The value will be stored as a predefined time interval.',
                     '', true, 'cron', null, null, 'daily, weekly, monthly', null);


            ]]>
        </sql>
    </changeSet>

</databaseChangeLog>
