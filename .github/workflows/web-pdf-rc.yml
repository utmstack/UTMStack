name: WEB PDF RC
on:
  push:
    branches: [ "rc" ]
    paths:
      - 'web-pdf/**'
  workflow_dispatch:
jobs:
  docker-etl-java:
    uses: ./.github/workflows/web-pdf-docker-java.yml
    with:
      image_name: web-pdf
      environment: v10-rc
      directory: web-pdf
