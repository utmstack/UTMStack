name: Web PDF RC
on:
  push:
    branches: [ "rc" ]
    paths:
      - 'web-pdf/**'
  workflow_dispatch:
jobs:
  docker-native-image-java:
    uses: ./.github/workflows/docker-graalvm.yml
    with:
      image_name: web-pdf
      environment: v10-rc
      directory: web-pdf
