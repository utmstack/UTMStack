name: Web PDF Release
on:
  push:
    tags: [ 'v*' ]
    paths:
      - 'web-pdf/**'
  workflow_dispatch:
jobs:
  docker-native-image-java:
    uses: ./.github/workflows/docker-graalvm.yml
    with:
      image_name: web-pdf
      environment: v10
      directory: web-pdf
