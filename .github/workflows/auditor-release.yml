name: AD Auditor Release
on:
  push:
    tags: [ 'v*' ]
    paths:
      - 'ad-auditor/**'
  workflow_dispatch:
jobs:
  docker-etl-java:
    uses: ./.github/workflows/docker-java.yml
    with:
      image_name: user-auditor
      environment: v10
      directory: user-auditor
