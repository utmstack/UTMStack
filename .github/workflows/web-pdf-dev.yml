name: WEB PDF DEV
on:
  push:
    branches: [ master ]
    paths:
      - 'web-pdf/**'
  workflow_dispatch:
jobs:
  docker-etl-java:
    uses: ./.github/workflows/web-pdf-docker-java.yml
    with:
      image_name: web-pdf
      environment: v10-dev
      directory: web-pdf
