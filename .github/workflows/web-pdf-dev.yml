name: Web PDF Dev
on:
  push:
    branches: [ main ]
    paths:
      - 'web-pdf/**'
  workflow_dispatch:
jobs:
  docker-image-java:
    uses: ./.github/workflows/docker-java.yml
    with:
      image_name: web-pdf
      environment: v10-dev
      directory: web-pdf
