name: Correlation QA

on:
  pull_request:
    branches: [ "rc" ]
    paths:
      - 'correlation/**'

jobs:
  docker:
    uses: ./.github/workflows/docker-golang.yml
    with:
      image_name: correlation
      environment: v10-qa
      directory: correlation
      binary: correlation
      branch: main
