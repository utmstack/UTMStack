name: Correlation QA

on:
  pull_request:
    branches: [ "rc" ]
    paths:
      - 'correlation/**'
  workflow_dispatch:

jobs:
  docker:
    uses: ./.github/workflows/docker-golang.yml
    with:
      image_name: correlation
      environment: v10-qa
      directory: correlation
      binary: correlation
