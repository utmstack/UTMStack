name: AD Auditor RC
on:
  push:
    branches: [ "rc" ]
    paths:
      - 'ad-auditor/**'
  workflow_dispatch:
jobs:
  docker-etl-java:
    uses: ./.github/workflows/docker-java.yml
    with:
      image_name: user-auditor
      environment: v10-rc
      directory: user-auditor
