name: WEB PDF QA
on:
  pull_request:
    branches: [ "rc" ]
    paths:
      - 'web-pdf/**'
  workflow_dispatch:
jobs:
  docker-native-image-java:
    uses: ./.github/workflows/docker-java-native.yml
    with:
      image_name: web-pdf
      environment: v10-qa
      directory: web-pdf
