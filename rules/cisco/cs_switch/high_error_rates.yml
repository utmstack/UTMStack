- id: 1013
  dataTypes:
    - cs_switch
  name: High Error Rate Detected on Switch
  impact:
    confidentiality: 1
    integrity: 2
    availability: 3
  category: Network Performance
  technique: "T1498 - Network Denial of Service"
  adversary: origin
  references:
    - https://www.cisco.com/c/en/us/td/docs/switches/lan/catalyst9200/software/release/17-16/configuration_guide/sys_mgmt/b_1716_sys_mgmt_9200_cg/configuring_system_message_logs.html
    - https://attack.mitre.org/techniques/T1498/
  description: |
    Detects high error rates on Cisco switches which may indicate network issues, hardware problems, or potential denial of service conditions.
    
    Next Steps:
    1. Check the affected switch interface for physical connectivity issues (loose cables, damaged ports)
    2. Review interface statistics (show interface [interface-name]) for CRC errors, collisions, or other anomalies
    3. Verify duplex and speed settings match on both ends of the connection
    4. Check for recent configuration changes on the affected interface
    5. Monitor CPU and memory utilization on the switch (show processes cpu, show memory)
    6. Review switch logs for additional error messages or patterns
    7. If errors persist, consider temporarily disabling the interface to prevent cascading failures
    8. Check for potential DoS attacks or abnormal traffic patterns targeting the switch
  where: |
    safe(log.severity, 10) <= 3 &&
    (contains(safe(log.message, ""), "error rate exceed") ||
     contains(safe(log.message, ""), "excessive errors") ||
     contains(safe(log.message, ""), "error threshold") ||
     safe(log.mnemonic, "") == "EXCESSIVE_ERRORS" ||
     safe(log.mnemonic, "") == "ERR_THRESHOLD" ||
     (safe(log.facility, "") == "LINEPROTO" && contains(safe(log.message, ""), "error")))
  afterEvents:
    - indexPattern: v11-log-cisco-switch-*
      with:
        - field: target.device.keyword
          operator: filter_term
          value: '{{target.device}}'
        - field: log.interface.keyword
          operator: filter_term
          value: '{{log.interface}}'
      within: now-5m
      count: 10
  deduplicateBy:
    - lastEvent.target.device
    - lastEvent.log.interface
