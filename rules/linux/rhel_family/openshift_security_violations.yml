# Rule version v2.0.0

dataTypes:
  - linux
name: OpenShift Security Violations
impact:
  confidentiality: 3
  integrity: 3
  availability: 2
category: Credential Access
technique: "T1552.007 - Unsecured Credentials: Container API"
adversary: origin
references:
  - https://docs.openshift.com/container-platform/latest/security/index.html
  - https://attack.mitre.org/techniques/T1552/007/
description: |
  Detects security violations in OpenShift Container Platform including unauthorized API access, RBAC violations, security context constraint breaches, and malicious pod deployments. These violations indicate potential security breaches or misconfigurations that could lead to unauthorized access or privilege escalation within the cluster.

  The rule reads OpenShift and Kubernetes components that log to the journal (kubelet, kubenswrapper, hyperkube, CRI-O, and control planes that run as system services, such as k3s, RKE2 and MicroK8s). It matches authorization denials, security context constraint and Pod Security rejections, and admission webhook denials. Denials for node and control-plane identities (system:node:, system:kube-) are left out because they are routine while a cluster starts or reschedules.

  Next Steps:
  1. Immediately investigate the user account and source IP involved in the violation
  2. Review the specific OpenShift namespace and resources being accessed
  3. Check RBAC configurations and Security Context Constraints (SCC) policies
  4. Examine pod specifications for security violations or privileged access attempts
  5. Verify API audit logs for additional unauthorized access patterns
  6. Review admission controller and webhook configurations
  7. Consider temporarily restricting access for the affected user/service account
  8. Document findings and update security policies if needed
where: |
  (
    contains("log.syslogIdentifier", "openshift") ||
    contains("log.syslogIdentifier", "kube") ||
    oneOf("log.syslogIdentifier", ["k3s", "rke2", "crio"])
  ) &&
  (
    (contains("log.message", "is forbidden: User ") &&
     !contains("log.message", "system:node:") &&
     !contains("log.message", "system:kube-")) ||
    contains("log.message", "unable to validate against any security context constraint") ||
    contains("log.message", "violates PodSecurity") ||
    (contains("log.message", "admission webhook ") && contains("log.message", " denied the request")) ||
    contains("log.message", "RBAC DENY:")
  )
deduplicateBy:
  - dataSource
  - lastEvent.log.syslogIdentifier
