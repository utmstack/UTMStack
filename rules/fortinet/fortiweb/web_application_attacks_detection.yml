# Rule version v1.1.0

dataTypes:
- firewall-fortiweb
name: FortiWeb Web Application Attack Detection
impact:
  confidentiality: 3
  integrity: 3
  availability: 2
category: Initial Access
technique: T1190 - Exploit Public-Facing Application
adversary: origin
references:
- https://docs.fortinet.com/document/fortiweb/7.6.0/administration-guide/981691/syntax-based-sql-xss-injection-detection
- https://attack.mitre.org/techniques/T1190/
description: |
  Detects repeated Generic Attacks (including Extended) classifications from FortiWeb against the same application. Dedicated SQLi, XSS Known Exploits and malware rules handle those classes. Routine geographic/IP-reputation blocks are excluded.

  Both Alert (log-only) and explicit blocking decisions are covered. Check log.action and actionResult to distinguish detection from prevention; neither establishes successful exploitation. Review log.subtype, log.attacktype, log.signatureid and log.msg, then validate the request and outcome in application logs before escalating.
where: |
  exists("origin.ip") && exists("target.ip") && equalsIgnoreCase("log.type", "attack") &&
  regexMatch("log.action", "(?i)^(alert|alert_deny|deny|denied|block|blocked|period_block)$") &&
  regexMatch("log.subtype", "(?i)^Generic Attacks(?:\\s*\\(Extended\\))?$") &&
  exists("log.subtype")
afterEvents:
- indexPattern: v11-log-firewall-fortiweb-*
  with:
  - field: origin.ip
    operator: filter_term
    value: '{{.origin.ip}}'
  - field: target.ip
    operator: filter_term
    value: '{{.target.ip}}'
  - field: log.type
    operator: filter_term
    value: '{{.log.type}}'
  - field: log.subtype
    operator: filter_term
    value: '{{.log.subtype}}'
  - field: log.action
    operator: filter_term
    value: '{{.log.action}}'
  within: 15m
  count: 3
deduplicateBy:
- adversary.ip
- target.ip
- lastEvent.log.subtype
