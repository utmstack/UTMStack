# Rule version v1.0.0

dataTypes:
  - firewall-fortigate-traffic
name: Fortinet Antivirus Malware Outbreak Detection
impact:
  confidentiality: 3
  integrity: 3
  availability: 2
category: Initial Access
technique: "T1566 - Phishing"
adversary: origin
references:
  - https://docs.fortinet.com/document/fortigate/7.4.1/administration-guide/198726/antivirus
  - https://attack.mitre.org/techniques/T1566/
description: |
  Detects potential malware outbreak when multiple antivirus detections occur from the same source IP on the same appliance and virtual domain in a short time period. This indicates repeated blocked infected-file transfers, not proof of an infected client or successful delivery.

  Next Steps:
  1. Identify the specific malware detected by reviewing the virus field in the alert details
  2. Isolate the affected source IP addresses to prevent further spread
  3. Check if the blocked malware attempts indicate successful infections on any endpoints
  4. Review antivirus logs for the specific virus signature to understand the threat
  5. Scan all systems that communicated with the identified source IPs
  6. Update antivirus signatures and ensure all endpoints are protected
  7. Consider blocking the source IP at the perimeter if it's external
  8. Document all affected systems and remediation actions taken
where: |
  exists("log.devid") && !equals("log.devid", "") && exists("log.vd") && !equals("log.vd", "") &&
  (equals("log.type", "utm") && equals("log.subtype", "virus") &&
  equals("log.eventtype", "infected") && equals("actionResult", "denied") &&
  exists("origin.ip"))
afterEvents:
- indexPattern: v11-log-firewall-fortigate-traffic-*
  with:
  - field: log.devid
    operator: filter_term
    value: '{{.log.devid}}'
  - field: log.vd
    operator: filter_term
    value: '{{.log.vd}}'
  - field: origin.ip
    operator: filter_term
    value: '{{.origin.ip}}'
  - field: log.correlationCandidate.virusOutbreak
    operator: filter_term
    value: match
  within: 1h
  count: 5
groupBy:
- adversary.ip
- target.ip
- lastEvent.log.devid
- lastEvent.log.vd
