# Rule contract revision 2026-09-17; SDK v1.1.31.
dataTypes:
- aws
name: Repeated ECS Task API Requests by an Assumed Role
impact:
  confidentiality: 3
  integrity: 2
  availability: 1
category: System Integrity
adversary: origin
references:
- https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html
description: |
  Detects repeated successful DescribeTaskDefinition, RunTask or StartTask requests by an assumed role. These CloudTrail management calls do not establish access to the task credential endpoint or credential theft. Review the role, task and requested operations for authorization.
where: |
  (equals("log.awsRecordType","cloudtrail") &&
  (equals("log.eventSource", "ecs.amazonaws.com") &&
  oneOf("log.eventName", ["DescribeTaskDefinition", "RunTask", "StartTask"]) &&
  equals("actionResult","success") &&
  equals("log.userIdentity.type", "AssumedRole") &&
  contains("log.userIdentity.arn", ":assumed-role/"))) &&
  exists("dataSource") &&
  !equals("dataSource","") &&
  exists("log.awsAccountKeyType") &&
  !equals("log.awsAccountKeyType","") &&
  exists("log.awsAccountKey") &&
  !equals("log.awsAccountKey","") &&
  exists("origin.ip") &&
  !equals("origin.ip","")
afterEvents:
- indexPattern: v11-log-aws-*
  with:
  - field: dataSource
    operator: filter_term
    value: '{{.dataSource}}'
  - field: log.awsAccountKeyType
    operator: filter_term
    value: '{{.log.awsAccountKeyType}}'
  - field: log.awsAccountKey
    operator: filter_term
    value: '{{.log.awsAccountKey}}'
  - field: origin.ip
    operator: filter_term
    value: '{{.origin.ip}}'
  - field: log.correlationCandidate.aws_ecs_credential_theft
    operator: filter_term
    value: match
  within: 30m
  count: 5
groupBy:
- lastEvent.dataSource
- lastEvent.log.awsAccountKeyType
- lastEvent.log.awsAccountKey
- adversary.user
- lastEvent.log.sourceIPAddress
