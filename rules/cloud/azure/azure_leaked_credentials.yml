# Azure rule contract review v2.1.0
dataTypes:
- azure
name: Azure AD Leaked Credentials Detection
impact:
  confidentiality: 3
  integrity: 3
  availability: 2
category: Credential Access
technique: T1078 - Valid Accounts
adversary: origin
references:
- https://learn.microsoft.com/en-us/graph/api/resources/riskdetection?view=graph-rest-1.0
- https://learn.microsoft.com/en-us/entra/id-protection/concept-identity-protection-risks
- https://attack.mitre.org/techniques/T1078/
description: 'Detects Azure Identity Protection alerts for leaked credentials found on dark web, paste sites, or other sources.
  This indicates user credentials have been exposed and may be used for unauthorized access.


  Next Steps:

  1. Immediately force a password reset for the affected user

  2. Revoke all active sessions and refresh tokens

  3. Review recent sign-in activity for unauthorized access

  4. Check for any data access or configuration changes after the leak

  5. Enable MFA if not already required for the user

  6. Investigate how the credentials were leaked (phishing, malware, reuse)

  7. Check if the same password was used across other services

  '
where: '(equalsIgnoreCase("log.azureKind","risk") && oneOf("log.properties.riskEventType",["leakedCredentials"]) && !oneOf("log.properties.riskState",["dismissed",
  "remediated", "confirmedSafe"])) || (equalsIgnoreCase("log.azureKind","signin") && (exists("log.properties.riskEventTypes_v2.#(==\"leakedCredentials\")")
  || exists("log.properties.riskEventTypesv2.#(==\"leakedCredentials\")") || exists("log.properties.riskEventTypes.#(==\"leakedCredentials\")"))
  && !oneOf("log.properties.riskState",["dismissed", "remediated", "confirmedSafe"]))

  '
groupBy:
- lastEvent.dataSource
- lastEvent.log.azureScopeType
- lastEvent.log.azureScope
- lastEvent.log.azureOperation
- adversary.user
- adversary.ip
