# Sophos Central SIEM event review; impact ratings and history thresholds preserved.
dataTypes:
- sophos-central
name: Sophos Central Endpoint Threat or PUA Detection
impact:
  confidentiality: 3
  integrity: 3
  availability: 2
category: Threat Detection
technique: Vendor threat detection
adversary: origin
references:
- https://docs.sophos.com/central/customer/help/en-us/ManageYourProducts/LogsReports/Logs/Events/EventTypes/
description: Sophos reports an endpoint threat, potentially unwanted application, or failed PUA cleanup. A PUA can
  be legitimate software restricted by policy. Completed cleanup and dismissed events are excluded.
where: '((oneOf("log.type", ["Event::Endpoint::Threat::Detected", "Event::Endpoint::Threat::HIPSDetected", "Event::Endpoint::Threat::PuaDetected"])
  ||

  (oneOf("log.severity", ["high", "critical"]) && equals("log.category", "threat") && exists("log.threatName")))
  || oneOf("log.type",["Event::Endpoint::CorePuaDetection","Event::Endpoint::CorePuaCleanFailed"])) && !regexMatch("log.type","(?i)(clean|cleanedup|resolved|dismissed|cancelled|success)$")'
groupBy:
- lastEvent.dataSource
- lastEvent.log.customerId
- lastEvent.log.endpointId
- lastEvent.log.type
- target.host
- target.ip
