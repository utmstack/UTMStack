# Sophos Central SIEM event review; impact ratings and history thresholds preserved.
dataTypes:
- sophos-central
name: Sophos Central Ransomware Detection
impact:
  confidentiality: 3
  integrity: 3
  availability: 3
category: Ransomware Activity
technique: T1486 - Data Encrypted for Impact
adversary: origin
references:
- https://docs.sophos.com/central/customer/help/en-us/ManageYourProducts/LogsReports/Logs/Events/EventTypes/
- https://attack.mitre.org/techniques/T1486/
description: Sophos reports ransomware behavior or blocked encryption. Review vendor details and affected files;
  detection or prevention does not prove successful encryption.
where: '(oneOf("log.type", ["Event::Endpoint::Threat::RansomwareDetected", "Event::Endpoint::CryptoGuard::FileEncryption"])
  ||

  (equals("log.name", "CryptoGuard") && equals("log.action", "blocked")) ||

  (equals("log.name", "Ransomware detected") && equals("log.severity", "critical")) ||

  (equals("log.type", "Event::Endpoint::Threat::Detected") && equals("log.threatType", "Ransomware"))) && !regexMatch("log.type","(?i)(clean|cleanedup|resolved|dismissed|cancelled|success)$")'
groupBy:
- lastEvent.dataSource
- lastEvent.log.customerId
- lastEvent.log.endpointId
- lastEvent.log.type
- target.host
- target.ip
