# Reusable control (NIST 800-53 r5). TODO: define real check(s) against the data — placeholder, not evaluated.
id: sc-51
family: sc
familyName: System and Communications Protection
name: Hardware-based Protection
scope: data
statement: |-
  Employ hardware-based, write-protect for [system firmware components] ; and
    Implement specific procedures for [authorized individuals] to manually disable hardware write-protect for firmware modifications and re-enable the write-protect prior to returning to operational mode.
source: NIST SP 800-53 Rev 5 (public domain)
checks:
  - key: todo
    name: 'TODO: define compliance check for this control'
    todo: true            # Pending — evaluator shows "Not implemented", excluded from score
