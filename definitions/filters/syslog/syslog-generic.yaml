# Generic syslog filter, version 2.0.0
# Allow processing of any syslog log
pipeline:
  - dataTypes: [syslog]
    order: 31  # global pipeline order -- unique across all filters, customer-adjustable
    steps:
      - grok:
          patterns:
            - fieldName: log.message
              pattern: '(.*)'
          source: raw